# PostgreSQL connection string (NeonDB/Supabase/Postgres)
DATABASE_URL='postgresql://username:password@host:port/database_name'

# JWT settings
JWT_SECRET=your-very-long-random-secret-key-change-me
JWT_EXPIRY_HOURS=24

# Server port (Render/Railway inject this automatically)
PORT=8080
